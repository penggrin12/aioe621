name: "Publish release to PyPI"

on:
  workflow_dispatch:
  push:
    tags:
      - v*

jobs:
  run:
    runs-on: ubuntu-latest
    environment:
      name: pypi
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990  # v8.3.2
      - name: Install Python 3.10
        run: uv python install 3.10
      - name: Build
        run: uv build
      - name: Unittest (wheel)
        run: uv run --isolated --no-project --with dist/*.whl --with respx>=0.23.1 python -m unittest tests.test_mock
      - name: Unittest (source distribution)
        run: uv run --isolated --no-project --with dist/*.tar.gz --with respx>=0.23.1 python -m unittest tests.test_mock
      - name: Publish
        run: uv publish